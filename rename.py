import os
import time

# f_path = "./dataset"
# data = enumerate(os.listdir(f_path))
# for idx, val in data:
# 	old_file = os.path.join(f_path, val)
# 	new_file = os.path.join(f_path, str(idx) + "-" + str(time.time_ns()) + ".jpg")
# 	print(idx, val)
# 	os.rename(old_file, new_file)
